#include <iostream>
using namespace std;

bool es_mono(int x, int y, int z)
{
    //comencem definint el cas base per a la recursivitat
    if (x == 0 && y == 0 && z == 0) {
        return;
    } else // en aquest pas es on definim la fucnio recursiva
    {

        es_mono
    }
}

int main()
{
    int x, y, z;
    while (cin >> x >> y >> z)
        cout << es_mono(x, y, z) << endl;
}
