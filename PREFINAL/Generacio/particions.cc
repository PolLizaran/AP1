#include <iostream>
#include <string>
#include <vector>
using namespace std;

void escriure();

int main()
{
    int n;
    cin >> n;
    string s;
    vector<string> words(n);
    for (k = 0; k < n; ++k)
        cin >> words[k];
}
