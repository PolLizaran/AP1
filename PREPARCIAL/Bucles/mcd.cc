#include <iostream>
using namespace std;
int main() {
  // Resoldre mitjançant la funció d'Euclides.
  int a, b;
  cin >> a >> b;
  if (a == b) {
    cout << a << endl;
  }
  if (a > b) {
    int n = a % b;
  }
}
